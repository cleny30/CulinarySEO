export interface CategoryCount {
    categoryId: number,
    categoryName: string,
    slug: string,
    productCount: number 
}
export interface Category {
  categoryId: number;
  categoryName: string;
  categoryImage: string;
  description: string;
  slug: string;
}